Running:
Can run a python local exe file, or a static HTML website on, say github, which allows all devices to open it no problem

Necessary Features:
- JSON files with data for each template, which indicates the fields available, their default size, and coordinates.
- The maximum character limit per each field. (By Default depends on the font size and field)

Future Features:
-
- 
